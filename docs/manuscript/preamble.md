# Rendering Preamble

The publication renderer consumes `preamble.tex`. This Markdown companion follows the template project convention and documents the packages used for equations, tables, figures, links, and typography.
